{
  "name": "nexora-workspace",
  "version": "1.0.0",
  "description": "Nexora — Intelligent workspace for understanding projects, tasks and team activity",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue.bold,cyan.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "start": "npm run start --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "mern",
    "react",
    "typescript",
    "vite",
    "tailwind",
    "framer-motion",
    "mongodb",
    "express",
    "saas"
  ],
  "author": "Nexora Team",
  "license": "MIT"
}
